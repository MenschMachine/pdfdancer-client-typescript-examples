- `.propagate-context.md` was referenced by `.propagate-prompt.md` but is missing from this workspace.
- `npm install github:MenschMachine/pdfdancer-client-typescript#<commit>` did not produce the SDK `dist/` artifacts in `node_modules`, so the examples repo could not typecheck or run until the upstream commit was cloned and built manually, then its generated `dist/` was copied into the installed package for local verification.
- The local Docker image `ghcr.io/menschmachine/pdfdancer-api:pr-68` did not start with a plain `docker run -p 8080:8080 ...`; it required `PDFDANCER_API_KEY_ENCRYPTION_SECRET` to be set, and startup also expected a `./fonts` directory to exist. Those requirements were not documented in the propagation prompt.
