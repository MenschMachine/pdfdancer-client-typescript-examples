Quickstart examples for Showcase.pdf. Each script exports a `runExample()` helper
so you can import snippets elsewhere or run them directly.

- `01_inspect_document.ts` – inspect pages, paragraphs, images, and form fields.
- `02_extract_text.ts` – dump every paragraph into `output/quickstart/extracted_text.txt`.
